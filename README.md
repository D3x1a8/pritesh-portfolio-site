# Pritesh Portfolio (static)

A simple, fast portfolio site (plain HTML/CSS/JS). Free to host on GitHub Pages / Cloudflare Pages.

## Customize
Edit `site.config.js` and the content in `index.html`.

## Local preview
Just open `index.html` in a browser.
